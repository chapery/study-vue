import Vue from 'vue'
import App from './App'

Vue.config.productionTip = false

/* eslint-disable no-unused-vars  */
let app = new Vue({
    el: '#app',
    template: '<App/>',
    components: { App }
})
